{
  "name": "reopsy-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-dependency ingest pipeline for ReOpSy (OpenAlex + arXiv -> Supabase)",
  "type": "commonjs",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "ingest": "node ingest/ingest.js",
    "dry": "node ingest/ingest.js --dry --topics ml --limit 5",
    "test": "node --test \"ingest/test/*.test.js\""
  },
  "license": "UNLICENSED",
  "dependencies": {
    "node-cron": "^4.6.0",
    "sqlite3": "^6.0.1"
  }
}
